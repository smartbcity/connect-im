import { Meta } from '@storybook/addon-docs/blocks'
import SpaceEndpointRootIndex from './space-endpoint-root/index.mdx'

<Meta
  title={"SpaceEndpoint"}
  parameters={
    {
      previewTabs: {
        canvas: {
          hidden: true
        }
      }
    }
  }
/>

<SpaceEndpointRootIndex />
