{
    "space": "${KC_REALM}",
    "theme": "${KC_REALM_THEME}",
    "smtp": {
        "host": "${SMTP_HOST}",
        "port": "${SMTP_POST}",
        "from": "noreply@smartb.city",
        "ssl": "false",
        "starttls": "false",
        "auth": "false"
    },
    "adminUsers": [],
    "imMasterClient": {
        "clientId": "${KC_IM_CLIENT_ID}",
        "clientSecret": "${KC_IM_CLIENT_SECRET}"
    }
}
